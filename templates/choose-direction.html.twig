{% extends 'base.html.twig' %}

{% block body %}
    <div class="directions-block">
        <ul class="list">
            {% for direction in directions %}
                <li class="item">
                    <a href="{{ path('app_difficulties', {'name': direction.name}) }}">{{ direction.name }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}

{% block javascripts %}

{% endblock %}

